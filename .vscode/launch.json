{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Cassino",
            "program": "${workspaceFolder}",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
