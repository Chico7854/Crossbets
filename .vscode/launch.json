{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "Debug Cassino",
            "program": "${workspaceFolder}/cassino",   // Ensure this path points to your compiled executable
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",                           // Set to GDB
            "miDebuggerPath": "/usr/bin/gdb",    // Replace with actual path to gdb.exe
            "externalConsole": true,
            "stopAtEntry": false
        }
    ]
}
